
import 'package:flutter/material.dart';

class Notifcation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Notification Data view here')),
    );
  }
}
